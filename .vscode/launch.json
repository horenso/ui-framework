{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/zig-out/bin/ui_framework",
      "preLaunchTask": "zig build install",
      "args": ["./src/main.zig"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
